app = "sshfolio"
primary_region = "sin"  # Singapore — close to India

[build]
  dockerfile = "Dockerfile"

[[services]]
  internal_port = 22097
  protocol = "tcp"

  [[services.ports]]
    port = 22097
